name: Daily Update

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Manual run reason (optional)"
        required: false
        default: "manual"

      daily_only:
        description: "Run Daily Digest report only?"
        required: true
        default: "false"
        type: choice
        options: ["false", "true"]

      voc_only:
        description: "Run Crema VOC only?"
        required: true
        default: "false"
        type: choice
        options: ["false", "true"]

      blog_only:
        description: "Run Blog VOC only?"
        required: true
        default: "false"
        type: choice
        options: ["false", "true"]

      debug:
        description: "Enable debug logs? (prints more logs, keeps extra debug json)"
        required: true
        default: "false"
        type: choice
        options: ["false", "true"]

      owned_backfill:
        description: "Backfill OWNED portal from 2025-01-01 to today? (warning: can be heavy)"
        required: true
        default: "false"
        type: choice
        options: ["false", "true"]

  schedule:
    # KST 06:30 = UTC 21:30 (전날)
    - cron: "30 21 * * *"

concurrency:
  group: daily-update-main
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Pull latest main (rebase)
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main

      - name: Validate dispatch inputs (only one true)
        if: ${{ github.event_name == 'workflow_dispatch' }}
        shell: bash
        run: |
          set -euo pipefail
          cnt=0
          [ "${{ inputs.daily_only }}" = "true" ] && cnt=$((cnt+1))
          [ "${{ inputs.voc_only }}"   = "true" ] && cnt=$((cnt+1))
          [ "${{ inputs.blog_only }}"  = "true" ] && cnt=$((cnt+1))
          if [ "$cnt" -gt 1 ]; then
            echo "[ERROR] Only one of daily_only/voc_only/blog_only can be true."
            exit 1
          fi

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install -r requirements.txt

          # BigQuery/Pandas
          pip install db-dtypes pyarrow google-cloud-bigquery pandas

          # HTML→PNG 렌더링용
          pip install playwright
          playwright install --with-deps chromium

      - name: Ensure reports dir
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p reports

      - name: Set KST time (for badge/meta + yesterday)
        id: set_time
        shell: bash
        run: |
          set -euo pipefail
          echo "kst=$(TZ=Asia/Seoul date '+%Y.%m.%d (%a) %H:%M KST')" >> $GITHUB_OUTPUT
          echo "kst_date=$(TZ=Asia/Seoul date '+%Y-%m-%d')" >> $GITHUB_OUTPUT
          echo "yesterday_ymd=$(TZ=Asia/Seoul date -d 'yesterday' '+%Y-%m-%d')" >> $GITHUB_OUTPUT

      # =========================================================
      # Daily Digest Report (어제 + YoY만 생성) — HUB FIXED (허브는 고정)
      # =========================================================
      - name: Build Daily Digest (daily+weekly) — reports
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.voc_only != 'true' }}
        shell: bash
        env:
          GA4_PROPERTY_ID: ${{ secrets.GA4_PROPERTY_ID }}
          GOOGLE_SA_JSON_B64: ${{ secrets.GOOGLE_SA_JSON_B64 }}
          KST_STAMP: ${{ steps.set_time.outputs.kst }}
          YMD: ${{ steps.set_time.outputs.yesterday_ymd }}

          # 리포트는 덮어쓰기 허용
          DAILY_DIGEST_SKIP_IF_EXISTS: "false"

          # 허브는 고정
          DAILY_DIGEST_SKIP_HUB_WRITE: "true"
        run: |
          set -euo pipefail

          if [ -z "${GA4_PROPERTY_ID:-}" ]; then
            echo "[WARN] GA4_PROPERTY_ID is empty. skip."
            exit 0
          fi
          if [ -z "${GOOGLE_SA_JSON_B64:-}" ]; then
            echo "[WARN] GOOGLE_SA_JSON_B64 is empty. skip."
            exit 0
          fi

          echo "$GOOGLE_SA_JSON_B64" | base64 -d > /tmp/ga_sa.json
          export GOOGLE_APPLICATION_CREDENTIALS=/tmp/ga_sa.json

          mkdir -p reports/daily_digest/daily reports/daily_digest/weekly reports/daily_digest/data/daily reports/daily_digest/data/weekly

          YOY="$(TZ=Asia/Seoul date -d "${YMD} - 364 days" '+%Y-%m-%d')"
          echo "[INFO] Build only: YMD=${YMD}, YOY=${YOY}"

          # ✅ 중요: rm로 산출물 삭제하지 말 것 (생성 실패 시 404 방지)
          # ✅ 중요: python 실패를 숨기지 말 것 (|| true 제거)

          export GA4_START_DATE="${YMD}"
          export GA4_END_DATE="${YMD}"
          python -u daily_digest_live_final_static_hub.py --out-dir reports/daily_digest

          export GA4_START_DATE="${YOY}"
          export GA4_END_DATE="${YOY}"
          python -u daily_digest_live_final_static_hub.py --out-dir reports/daily_digest

          if [ -f reports/daily_digest/index.html ]; then
            echo "<!-- BUILD_STAMP: ${KST_STAMP} -->" >> reports/daily_digest/index.html
          fi

          echo "[INFO] Outputs:"
          ls -la reports/daily_digest/daily | tail -n 40 || true
          ls -la reports/daily_digest/weekly | tail -n 40 || true
          ls -la reports/daily_digest/data/daily | tail -n 40 || true
          ls -la reports/daily_digest/data/weekly | tail -n 40 || true

      # =========================================================
      # Render yesterday daily HTML -> PNG (for email)
      # =========================================================
      - name: Render yesterday Daily Digest PNG
        if: ${{ success() }}
        continue-on-error: true
        shell: bash
        env:
          YMD: ${{ steps.set_time.outputs.yesterday_ymd }}
        run: |
          set -euo pipefail
          HTML="reports/daily_digest/daily/${YMD}.html"
          PNG="reports/daily_digest/daily/${YMD}.png"

          if [ ! -f "$HTML" ]; then
            echo "[WARN] Daily HTML not found for PNG render: $HTML"
            ls -la reports/daily_digest/daily || true
            exit 0
          fi

          python -u scripts/render_daily_png.py "$HTML" "$PNG" || true
          ls -la "$PNG" || true

      # ----------------------------
      # 1) Naver Lowest Price
      # ----------------------------
      - name: Run Naver Lowest Price crawler
        if: ${{ github.event_name != 'workflow_dispatch' || (inputs.daily_only != 'true' && inputs.voc_only != 'true' && inputs.blog_only != 'true') }}
        continue-on-error: true
        env:
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
        shell: bash
        run: |
          set -euo pipefail
          python -u naver_crawl_columbia_c6c7_title_min.py             --limit 999999             --output_html reports/naver_lowest_price.html             --history_dir .             --cache_dir .naver_cache             || true
          ls -la reports/naver_lowest_price.html || true

      # ----------------------------
      # 2) Hero banner
      # ----------------------------
      - name: Run Hero banner crawler
        if: ${{ github.event_name != 'workflow_dispatch' || (inputs.daily_only != 'true' && inputs.voc_only != 'true' && inputs.blog_only != 'true') }}
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          HTML_USE_ABSOLUTE_FILE_URL=0 OUT_DIR=reports python -u hero_main_top3_final_PRETTY_WHITE_FINAL_FIXED.py || true
          ls -la reports/hero_main.html || true

      # ----------------------------
      # 3) External Signal (VOC)
      # ----------------------------
      - name: Run External Signal (VOC)
        if: ${{ github.event_name != 'workflow_dispatch' || (inputs.daily_only != 'true' && inputs.voc_only != 'true' && inputs.blog_only != 'true') }}
        continue-on-error: true
        shell: bash
        env:
          DEBUG: ${{ github.event_name == 'workflow_dispatch' && inputs.debug == 'true' }}
          TARGET_DAYS: 7
        run: |
          set -euo pipefail
          python -u external_signal_combined.py || true
          ls -la reports/external_signal.html || true

      # ----------------------------
      # 4) Crema VOC
      # ----------------------------
      - name: Run Crema VOC (reviews + images + dashboard)
        if: ${{ github.event_name != 'workflow_dispatch' || (inputs.daily_only != 'true' && inputs.blog_only != 'true') }}
        continue-on-error: true
        env:
          CREMA_SECURE_DEVICE_TOKEN: ${{ secrets.CREMA_SECURE_DEVICE_TOKEN }}
          OUTPUT_TZ: Asia/Seoul
          PROJECT_ROOT: ${{ github.workspace }}
        shell: bash
        run: |
          set -euo pipefail

          INPUT_JSON="site/data/reviews.json"
          if [ ! -f "$INPUT_JSON" ]; then
            echo "[WARN] Missing input JSON: $INPUT_JSON (skip crema)"
            exit 0
          fi

          rm -rf reports/voc_crema || true
          mkdir -p reports/voc_crema

          python -u crema_voc.py --input "$INPUT_JSON" || true

          if [ -f "site/index.html" ]; then
            rsync -a --delete site/ reports/voc_crema/ || true
          else
            echo "[WARN] site/index.html not found after run (skip)"
            exit 0
          fi

          ls -la reports/voc_crema/index.html || true

      # ----------------------------
      # 4.5) Naver Blog VOC
      # ----------------------------
      - name: Run Naver Blog VOC (Columbia mentions)
        if: ${{ github.event_name != 'workflow_dispatch' || (inputs.daily_only != 'true' && inputs.voc_only != 'true') }}
        continue-on-error: true
        shell: bash
        env:
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
        run: |
          set -euo pipefail

          rm -rf reports/voc_blog || true
          mkdir -p reports/voc_blog

          python -u build_blog_voc_report.py             --queries "콜롬비아" "컬럼비아" "Columbia" "콜롬비아 자켓" "콜롬비아 패딩" "콜롬비아 양말"             --days 60             --per-query 100             --headless             --out-dir reports/voc_blog             || true

          ls -la reports/voc_blog/index.html || true

      # ----------------------------
      # 5) Summary
      # ----------------------------
      - name: Build Summary (reports/index.html + meta.json)
        if: ${{ github.event_name != 'workflow_dispatch' || (inputs.daily_only != 'true' && inputs.voc_only != 'true' && inputs.blog_only != 'true') }}
        continue-on-error: true
        env:
          UPDATED_KST: ${{ steps.set_time.outputs.kst }}
        shell: bash
        run: |
          set -euo pipefail
          python -u build_summary.py || true
          ls -la reports/index.html reports/meta.json || true

      # =========================================================
      # (NEW) OWNED Campaign Portal (EDM/LMS/KAKAO → products)
      # =========================================================
      - name: Build OWNED campaign portal (products by send_id)
        if: ${{ github.event_name != 'workflow_dispatch' || (inputs.daily_only != 'true' && inputs.voc_only != 'true' && inputs.blog_only != 'true') }}
        continue-on-error: true
        shell: bash
        env:
          GOOGLE_SA_JSON_B64: ${{ secrets.GOOGLE_SA_JSON_B64 }}
          BQ_PROJECT: columbia-ga4
          BQ_DATASET: analytics_358593394
          OWNED_BACKFILL: ${{ github.event_name == 'workflow_dispatch' && inputs.owned_backfill == 'true' }}
        run: |
          set -euo pipefail

          if [ -z "${GOOGLE_SA_JSON_B64:-}" ]; then
            echo "[WARN] GOOGLE_SA_JSON_B64 empty -> skip owned portal"
            exit 0
          fi

          echo "$GOOGLE_SA_JSON_B64" | base64 -d > /tmp/ga_sa.json
          export GOOGLE_APPLICATION_CREDENTIALS=/tmp/ga_sa.json

          mkdir -p reports/owned_portal

          if [ "${OWNED_BACKFILL}" = "true" ]; then
            echo "[INFO] OWNED backfill from 2025-01-01"
            python -u build_owned_campaign_portal.py               --project "${BQ_PROJECT}"               --dataset "${BQ_DATASET}"               --start 2025-01-01               --site-dir reports/owned_portal               || true
          else
            echo "[INFO] OWNED recent-days=7"
            python -u build_owned_campaign_portal.py               --project "${BQ_PROJECT}"               --dataset "${BQ_DATASET}"               --recent-days 7               --site-dir reports/owned_portal               || true
          fi

          ls -la reports/owned_portal || true
          ls -la reports/owned_portal/data/owned | head -n 60 || true

      # ----------------------------
      # 7) Commit & push results (safe rebase + retry)
      # ----------------------------
      - name: Commit & push results
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add -A reports site .naver_cache || true

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git fetch origin main

          CURRENT_BRANCH="$(git rev-parse --abbrev-ref HEAD || true)"
          echo "[INFO] current branch: ${CURRENT_BRANCH}"

          git pull --rebase --autostash origin main

          git add -A reports site .naver_cache || true

          if git diff --cached --quiet; then
            echo "No changes to commit after rebase"
            exit 0
          fi

          git commit -m "Daily auto update (reports)" || true

          for i in 1 2 3; do
            echo "[INFO] push attempt $i/3"
            if git push origin HEAD:main; then
              echo "[OK] pushed"
              exit 0
            fi

            echo "[WARN] push rejected. Rebase with latest origin/main and retry..."
            git fetch origin main
            git pull --rebase --autostash origin main
          done

          echo "[ERROR] failed to push after retries"
          exit 1

      # ----------------------------
      # Success Email (PNG inline 우선 + 첨부)
      # ----------------------------
      - name: Send success email (yesterday daily report PNG)
        if: ${{ success() }}
        continue-on-error: true
        env:
          SMTP_HOST: ${{ vars.SMTP_HOST }}
          SMTP_PORT: ${{ vars.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          DAILY_RECIPIENTS: ${{ vars.DAILY_RECIPIENTS }}
          HUB_URL: ${{ vars.HUB_URL }}
          KST_STAMP: ${{ steps.set_time.outputs.kst }}

          MODE_DAILY_ONLY: ${{ github.event_name == 'workflow_dispatch' && inputs.daily_only == 'true' }}
          MODE_VOC_ONLY:   ${{ github.event_name == 'workflow_dispatch' && inputs.voc_only == 'true' }}
          MODE_BLOG_ONLY:  ${{ github.event_name == 'workflow_dispatch' && inputs.blog_only == 'true' }}

          REPORT_DATE: ${{ steps.set_time.outputs.yesterday_ymd }}

          DAILY_DIGEST_PNG_PATH: reports/daily_digest/daily/${{ steps.set_time.outputs.yesterday_ymd }}.png
          DAILY_DIGEST_HTML_PATH: reports/daily_digest/daily/${{ steps.set_time.outputs.yesterday_ymd }}.html
        shell: bash
        run: |
          set -euo pipefail

          echo "[DEBUG] REPORT_DATE=${REPORT_DATE}"
          echo "[DEBUG] DAILY_DIGEST_PNG_PATH=${DAILY_DIGEST_PNG_PATH}"
          echo "[DEBUG] DAILY_DIGEST_HTML_PATH=${DAILY_DIGEST_HTML_PATH}"

          if [ ! -f "${DAILY_DIGEST_PNG_PATH}" ]; then
            echo "[WARN] PNG not found. Listing daily dir:"
            ls -la reports/daily_digest/daily || true
          fi

          python -u scripts/send_daily_email.py || true

{
  "name": "csk-ecomm-dashboard",
  "private": true,
  "scripts": {
    "build": "python scripts/prepare_cloudflare_bundle.py && python scripts/dashboard_security.py --sanitize --root dist",
    "deploy": "npm run build && wrangler deploy --config wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
